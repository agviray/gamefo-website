import styled from 'styled-components';

export const StyledVideo = styled.div`
  position: relative;

  .imageContainer {
    position: absolute;
    top: 0;
    left: 0;

    img {
      width: 100%;
    }
  }

  .video {
    width: 100%;
    height: 100%;
  }
`;
